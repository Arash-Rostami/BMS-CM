Implement the "Desk Reference" feature for BMS-CM exactly as specified in `.claude/plan/desk-reference-integration-plan.md`. Read that file in full before writing anything — it is the authoritative spec, not a rough sketch. Every migration, model, controller, trait, config file, and Blade partial in it is final, verified code, not pseudocode to reinterpret. Where the plan gives literal code, use it as-is; where it gives a diff (§9, the 8 resource wirings), apply it exactly as described.

Build **Phase 1 only** (as scoped in the plan's Phasing section) — do not build Phase 2/3 items (landing-page tip rotator, self-check quiz, field-level deep links, admin adoption widget). Do not add anything not in the plan: no new abstractions, no service-class wrapper around the controller, no extra caching layer, no test suite unless one already covers these files. This project's own coding philosophy (CLAUDE.md) and this plan both explicitly reject scope creep here — minimal and exactly-as-specified is the goal, not "improved."

## Build order

1. **Data layer** — migration (`create_desk_references_table`) + `App\Models\DeskReference` (§2). Run `php artisan migrate`.
2. **Config registry** — `config/desk-reference.php`, all 8 entries (§5).
3. **Trait** — `app/Filament/Traits/HasDeskReferenceTab.php` (§3). Use the two-step `Lang::has()` + non-empty-content gating exactly as written — this closes a real bug an earlier draft had (documented inline in the plan, §10's Dry Run Trace Log).
4. **Controller + route** — `app/Http/Controllers/DeskReferenceController.php` + the `POST /desk-reference/acknowledge` route in `routes/web.php` (§4).
5. **Blade partial** — `resources/views/filament/desk-reference/panel.blade.php` (§6). Pay close attention to: the `x-effect` + `tab === 'desk-reference'` tracking trigger (NOT `x-init` — using `x-init` here silently breaks tracking for every resource at once; §6 explains exactly why, verified against Filament's actual Tabs component source), the `@php use Illuminate\Support\Js; use Illuminate\Support\Str; @endphp` import block (bare `Str::`/`Js::` calls fail in compiled Blade views without it), and the `?? ''` fallbacks on term/step fields.
6. **CSS** — append the `.dr-waveform` rule to the existing `resources/css/fi-custom.css` utility block (§6a). Do not create a new CSS file.
7. **Wiring** — apply §9's exact diff to all 8 files: `app/Filament/Resources/{PurchaseRequest,ProformaInvoice,RegisteredOrder,BankProfile,PurchaseOrder,Payment,Shipment,Custom}Resource.php`. Each gets the `HasDeskReferenceTab` import + trait use, the `->tabs([...])` wrapped in `array_filter([...])`, and `static::getDeskReferenceInfolistTab(),` appended after the existing `static::getExtraAttributesInfolistTab(),` line. Nothing else in these files changes.
8. **Shared UI strings** — add the `desk_reference` sub-array (7 keys, §6b) to `lang/en/resources/general/strings.php` and `lang/fa/resources/general/strings.php`.
9. **Content** — author all 4 groups (`request_approval`, `order_processing`, `procurement_payment`, `logistics`) in `lang/fa/deskReference/` and `lang/en/deskReference/` (fa + en only — **do not** create `lang/fr/deskReference/*`, that's the founder's own task later; `fallback_locale=en` already covers the gap). Source the content from `.claude/plan/guide-source.txt` per the exact line-range mapping in §1a — note the two corrections called out there (section 5 maps to `procurement_payment` not `order_processing`; `logistics` is fed by two non-adjacent source sections). Translate fa → en yourself for the en files; keep both faithful to the source, not paraphrased down.
10. **Doc sync** — update `app/Filament/filamentPattern.md` and `app/Utils/helpersPattern.md` per this project's own doc-sync policy (new trait, the `tabs(array_filter(...))` convention, the `deskReference/{group}.php` namespace, the `config/desk-reference.php` registry pattern), and append a dated entry to CLAUDE.md's Latest Changes section.

## Before declaring it done

Re-run the dry run from the plan's §10 against the **actual code you just wrote**, not just the plan — a dry run against a plan can miss things a dry run against literal code catches. Then go through the Pre-flight / Acceptance Checklist at the end of the plan item by item. Items marked **(verify)** need a real check (migrate, `npm run build`, opening a record in the browser and watching the Network tab) — do those. Items marked **(fixed in this plan)** just need confirming the fix is actually present in what you wrote, not silently simplified away.

Run `./vendor/bin/pint` and `npm run build` (required this time — the CSS changed) before finishing. No `php artisan test` run is expected unless you added tests yourself for these new files.

Media files (audio/video/posters) are **not** part of this implementation pass — the founder drops those in separately later (`resources/{audio,video,img}/desk-reference/`, exact names in the plan's §1b). The code must already handle their absence gracefully (it does, per the `media` key being fully optional) — don't block on them or fabricate placeholder files.

If anything in the plan looks wrong, ambiguous, or contradicts what you find in the actual source files while implementing — stop and flag it rather than guessing or silently deviating. The plan was built by reading real source files directly (not assumed), but code changes; re-verify anything version-sensitive (the `Filament\Schemas\Components\View` API, the Tabs rendering behavior) still matches what's actually installed.
