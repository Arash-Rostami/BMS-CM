<?php

$text = <<<'TEXT'
Doc-review-action policy for this project: alongside the code reviewer, a second PostToolUse gate at .claude/hooks/post_tool_doc_review.php fires on every Edit/Write to CLAUDE.md or any *Pattern.md pattern doc (filamentPattern.md, servicesPattern.md, localizationPattern.md, stylesPattern.md, scriptPattern.md, modelsPattern.md, helpersPattern.md, viewsPattern.md, widgetsPattern.md) — same claude-sonnet-4-6 / dual glm-5.2:cloud 93%-confidence-gate mechanism as the code reviewer, but scoped to doc quality: it runs a static grep-verification pass against the live repo on every backtick-quoted identifier in the changed text (flagging any that don't actually exist as a likely stale or fabricated claim) and then judges factual accuracy plus doc hygiene — no narrative or dated changelog framing, no content that duplicates or belongs in a different domain's pattern doc (this project consolidated CSS/JS/models/Filament/services/localization/widgets/views into one authoritative file each; everything else should hold only a one-line pointer), no code comments in snippets, no padding. It is an LLM judgment call layered on a cheap static check, not a deterministic linter, so it can still be wrong. Apply the exact same discipline as the code reviewer: before acting on any block, verify in your own judgment that the issue is genuinely present by checking the actual file and the identifiers it flagged, don't trust the block blindly. If it's a false positive, say briefly why you're not changing anything and stop. If real, fix it directly in the same turn — correct the stale fact, cut the duplication, or move the content to its owning pattern doc — rather than arguing with or bypassing the gate.
TEXT;

echo json_encode(['hookSpecificOutput' => ['hookEventName' => 'SessionStart', 'additionalContext' => $text]], JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE);
