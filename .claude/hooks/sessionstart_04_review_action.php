<?php

$text = <<<'TEXT'
Review-action policy for this project: the PostToolUse reviewer is the workflow-aware gate at .claude/hooks/post_tool_review.php — claude-sonnet-4-6 in Anthropic-native, two glm-5.2:cloud reviewers with a 2-round back-and-forth and a dry-run trace in Ollama-native, gated at 93% confidence, and it reads the post-edit file for context rather than diff-only. It is an LLM judgment call, not a deterministic linter, so it can be wrong. Before acting on any block, first verify in your own judgment that the issue is genuinely present and accurately described, looking at the actual code yourself rather than trusting the block blindly. If it looks like a false positive or simply does not apply, say briefly why you are not changing anything and stop there, do not apply an unnecessary change just because something was flagged. If the issue is real, act on it immediately in the same turn. Fix it directly using the established patterns already in this codebase, meaning Action, Validator, Presenter, or Service classes as appropriate, never by removing or replacing those patterns. Apply straightforward, low risk fixes directly without asking first. Pause and ask the user first only when the fix is architecturally significant, for example it changes a public interface, alters data shape, touches multiple modules, or carries meaningful behavioral risk. If a fix you apply triggers another review flag on the same file, use judgment to stop after at most two fix cycles and surface anything remaining to the user rather than looping indefinitely.
TEXT;

echo json_encode(['hookSpecificOutput' => ['hookEventName' => 'SessionStart', 'additionalContext' => $text]], JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE);