<?php

$text = <<<'TEXT'
Scope: this delegation policy governs the Anthropic-native Claude orchestration setup only. If the driving session model is glm-5.2:cloud, meaning the Ollama setup where GLM-5.2 is the main orchestrator, this delegation policy is inert and .claude/skills/ollama/SKILL.md governs coordination instead, with you acting as Lead GLM-5.2 running the orchestration pipeline for non-trivial work. Under either setup, delegating a code-gen slice to kimi-k2.7-code:cloud remains valid since it is not self-delegation. Model delegation policy for this project: handle all planning, architecture, and code review yourself, never delegate those. Skip delegation for one-liners or trivial edits, write those directly. For code-generation tasks you judge to be non-trivial low or mid complexity, first explicitly ask the user via a direct question whether to delegate that specific task to the glm-5.2:cloud model. Delegation is always optional and never mandatory, only proceed if the user clearly says yes to that exact question, otherwise write the code yourself. The delegation prompt sent to glm-5.2:cloud must instruct it to first read and follow .claude/skills/laravel-performance/SKILL.md and .claude/skills/code-reviewer/SKILL.md before writing any code. To delegate, send a chat completion request to the local Ollama server at http://localhost:11434/api/chat with model set to glm-5.2:cloud, that instruction plus a clear task prompt including relevant file context, and stream:false. Review the returned code yourself for correctness, security, and fit with the codebase before presenting or applying it. If the delegation attempt fails for any technical reason, such as a connection error, a timeout, the model not being pulled, or an invalid or empty response, do not retry repeatedly and do not leave the task unfinished, immediately fall back to writing the code yourself in this same turn, exactly as if delegation had never been attempted, and briefly tell the user the delegation attempt failed and you handled it directly instead. The same immediate fallback applies if the request technically succeeds but the returned code is clearly the wrong language, off topic, incomplete, or otherwise unusable for the task, treat that the same as a technical failure rather than trying to salvage or re-prompt it, write it yourself instead. Before delegating, always scan the relevant file context for secrets, credentials, API keys, tokens, passwords, connection strings, or other sensitive values, if any are present anywhere in that context, do not delegate at all regardless of what the user said yes to, write the code yourself instead and tell the user briefly why delegation was skipped.
TEXT;

echo json_encode(['hookSpecificOutput' => ['hookEventName' => 'SessionStart', 'additionalContext' => $text]], JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE);